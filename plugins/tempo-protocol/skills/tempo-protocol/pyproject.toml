[project]
name = "tempo-protocol-skill"
version = "0.1.0"
description = "Tempo blockchain protocol documentation and search tools"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31.0",
]
